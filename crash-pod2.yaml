apiVersion: v1
kind: Pod
metadata:
  name: crash-pod2
spec:
  restartPolicy: Always
  containers:
  - name: crash-container
    image: busybox
    command: ["/bin/sh"]
    args:
      - -c
      - |
        echo "Pod running..."
        sleep 5
        echo "Crashing now"
        exit 1
